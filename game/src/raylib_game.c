

#include "raylib.h"
#include "stellar.h"

int main(void)
{
    InitWindow(SCREEN_WIDTH, SCREEN_HEIGHT, GAME_NAME);

    SetTargetFPS(60);

    while (!WindowShouldClose())
    {

    }

    CloseWindow();

    return 0;
}
